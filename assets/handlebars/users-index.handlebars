{{#each awards as |award|}}
  <section data-id="{{award.id}}">
    <h6>Type:</h6>
    <h3>
      {{award.level}}
    </h3>
    <h6>Title of</h6><h6>{{award.name}}</h6>
    {{> partial id=award.id}}
  </section>
{{/each}}